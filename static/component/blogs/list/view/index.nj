{% import 'component/blogs/list-card/index.nj' as tmp %}
{% macro list(listData) %}
    <div class='blogs-list'>
        {% for item in listData %}
            {{tmp.card(item)}}
        {% endfor %}
    </div>
{% endmacro %}



